[unstable]
build-std-features = ["compiler-builtins-mem"]
build-std = ["core", "compiler_builtins", "alloc"]
json-target-spec = true

[build]
target = "x86_64-rsos-kernel.json"

[target.x86_64-rsos-kernel]
rustflags = [
  "-C", "link-arg=-Tkernel/link.ld",
]
